const express = require('express');
const axios = require('axios');
const cors = require('cors');
const fs = require('fs');
const path = require('path');
const crypto = require('crypto');
const { GoogleGenerativeAI } = require("@google/generative-ai");

const app = express();

// 1. 미들웨어 및 용량 설정 (대용량 등기부 PDF 수신 대비)
app.use(express.json({ limit: '100mb' }));
app.use(express.urlencoded({ limit: '100mb', extended: true }));
app.use(cors());
app.use(express.static('public'));

// 2. 리포트 저장 폴더 자동 생성
const REPORT_DIR = path.join(__dirname, 'public/reports');
if (!fs.existsSync(REPORT_DIR)) {
    fs.mkdirSync(REPORT_DIR, { recursive: true });
}

// ==========================================
// 3. 설정 정보 (HKey 및 하이픈 계정)
// ==========================================
const CONFIG = {
    HKEY: '130e008d28511f21',
    USER_ID: 'snow89',
    HYPHEN_ID: 'snow89',
    HYPHEN_PW: '10dnjf2djr!',
    PAY_NO: 'V37642074050',
    PAY_PW: '10dnjf',
    GEMINI_API_KEY: 'AIzaSyBQA5y7Ttpck8kQPiezs7Ti6geQ-yDxVAM', 
};

// ============================================================
// [최종본] 가독성 극대화 및 케이스별 정밀 로직 반영 프롬프트
// ============================================================
const DAMBAEK_PROMPT = `
# [담백스퀘어(주) 대출비서] 시스템 프롬프트

## 1. 기본 역할
- 사용자 입력 데이터와 등기부 데이터를 분석하여 대출 리포트를 작성한다.
- **출력 원칙:** 아래 [최종 리포트 템플릿]의 양식을 엄격히 준수한다. 
- **간격 유지:** 구분선(────────────────────────)의 위와 아래, 그리고 각 케이스(CASE) 사이에는 반드시 **'빈 줄(공백 라인)'을 삽입**하여 가독성을 높인다.
- **강조 표기:** 주요 금액과 한도는 **강조(Bold)** 처리한다.

## 2. 데이터 처리 및 우선순위
1. **기대출:** 사용자 입력값 절대 우선. (입력 없을 시 PDF 을구 기준)
2. **시세:** 사용자 입력값(KB시세) 사용.
3. **면적:** 사용자 입력값 우선 > 없으면 PDF 표제부 전유부분 면적 사용.

## 3. 케이스별 정밀 로직 가이드
- **[방공제]:** 서울(5500), 과밀(4800), 광역시/주요도시(2800), 기타(2500).
- **[Case 1] 생애최초:** MIN(KB시세 × 80%, 60,000만원). (최대 6억 상한 고정)
- **[Case 2] 일반매매:** - **규제지역:** 서울(전역), 경기 성남(분당/수정/중원), 수원(영통/장안/팔달), 용인(수지), 안양(동안), 과천, 광명, 의왕, 하남.
  - **규제지역 한도:** 시세 15억 이하(시세 40%, Max 6억) / 15억~25억(4억 고정) / 25억 초과(2억 고정).
  - **비규제지역:** 시세 × 70% (상한 없음).
- **[Case 3] 전세퇴거:** 규제지역(1주택 40%, 다주택 30%) / 비규제지역(모두 60%).

## 4. 최종 리포트 템플릿 (여백 및 강조 양식 엄격 준수)

────────────────────────

🏢 **담백스퀘어(주) 대출비서 리포트**

────────────────────────

📍 **1. 기본 정보**

* 소유자: **{소유자명}** 님
* 주소: {전체 주소}
* 전용면적: {전용면적}㎡
* KB시세: **{KB시세}만원**

────────────────────────

💳 **2. 기대출 및 자산 평가**

{순위}순위. {은행명}: 원금 {원금}만원 / 채권최고액 {채권액}만원 ({비율}%)

* **총 원금합계:** **{원금합계}만원**
* **총 채권최고액 합계:** **{채권최고액합계}만원**
* **순자산:** **{순자산}만원** (시세 - 총원금)
* **대출비율(LTV):** **{LTV비율}%** (현재 시세 대비 총 원금 비율)

**[대출 LTV 금액별 예상표]**

**ㅁ 상호금융 (방공제 적용)**
* LTV 80%: {LTV80_금액}만원 - {방공제액}만원 = **{계산결과}만원**
* LTV 85%: {LTV85_금액}만원 - {방공제액}만원 = **{계산결과}만원**

**ㅁ 저축은행/캐피탈 (방공제 미적용)**
* LTV 80%: **{LTV80_금액}만원**
* LTV 85%: **{LTV85_금액}만원**
* LTV 90%: **{LTV90_금액}만원**

────────────────────────

📊 **3. 대출 한도 및 월납입금 (예상)**


**[케이스1] 생애최초 매매잔금 (구입자금)**

* 한도: **{C1_한도}만원** (LTV 80% 이내, 최대 6억원 적용)
* 1금융: 금리 4.3%~ / 월 원리금 {C1_월1}만원 / 필요소득 약 **{C1_소득1}만원**
* 2금융: 금리 4.0%~ / 월 원리금 {C1_월2}만원 / 필요소득 약 **{C1_소득2}만원**


**[케이스2] 일반 매매잔금 (무주택/1주택 처분)**

* 한도: **{C2_한도}만원** ({지역구분} 적용 / {C2_상한로직} 기준)
* 1금융: 금리 4.3%~ / 월 원리금 {C2_월1}만원 / 필요소득 약 **{C2_소득1}만원**
* 2금융: 금리 4.0%~ / 월 원리금 {C2_월2}만원 / 필요소득 약 **{C2_소득2}만원**


**[케이스3] 전세퇴거자금 (보증금 반환)**

* ① **1주택자** (LTV {C3A_LTV}%): **{C3A_한도}만원** / 1금융소득 **{C3A_소득1}만원**
* ② **다주택자** (LTV {C3B_LTV}%): **{C3B_한도}만원** / 2금융소득 **{C3B_소득2}만원**
* 비고: {지역명} ({규제여부}) 기준 적용


**[케이스4] 사업자 담보대출 (후순위/생활자금)**

**ㅁ 상호금융 (LTV 80% - 방공제)**
* 한도: **{C4A_한도}만원**
* 예상금리: 4.9%~ / 월 이자: 약 **{C4A_이자}만원** (이자만 납부)
* 계산식: ({80%금액} - {방공제액}) - {선순위채권총액}

**ㅁ 저축/캐피탈 (LTV 90%)**
* 한도: **{C4B_한도}만원**
* 예상금리: 8.5%~ / 월 이자: 약 **{C4B_이자}만원** (이자만 납부)


────────────────────────

➕ **[케이스5] 후순위 추가 대출**

* 상호금융 (80%): 가용한도 **{C5A_한도}만원**
* 저축/캐피탈 (90%): 가용한도 **{C5B_한도}만원**
* 비고: 기존 선순위 채권최고액 {기존채권합계}만원 유지 가정


────────────────────────

🔄 **[케이스6] 통대환 (갈아타기)**

* 상호금융 (80%): 최대 **{C6A_한도}만원** / 여유자금 **{C6A_가용}만원**
* 저축/캐피탈 (90%): 최대 **{C6B_한도}만원** / 여유자금 **{C6B_가용}만원**
* 비고: 기존 대출 전액 상환 후 신규 실행 가정


────────────────────────

⚠️ **안내사항**

* 본 리포트는 입력하신 정보와 KB시세를 기반으로 한 단순 예상치입니다.
* 차주(고객)의 신용점수, 소득, 물건지 상황에 따라 실제 한도 및 금리는 달라질 수 있습니다.
* 정확한 심사를 위해 담당자에게 문의 바랍니다.

📞 **상담문의: 박순호 대표 (010-3950-6886)**
**담백스퀘어(주) 대출비서**

────────────────────────
`;

// AI 모델 초기화
const genAI = new GoogleGenerativeAI(CONFIG.GEMINI_API_KEY);
const model = genAI.getGenerativeModel({
    model: "gemini-2.0-flash",
    systemInstruction: DAMBAEK_PROMPT
});

// ==========================================
// 5. 전용면적 정밀 추출 함수
// ==========================================
function extractExclusiveArea(outList) {
    if (!outList || !Array.isArray(outList)) return null;
    const cleanText = outList.map(item => JSON.stringify(item)).join('').replace(/\\n/g, '').replace(/\s/g, '');
    const sections = cleanText.split('전유부분');
    if (sections.length < 2) return null;
    const match = sections[1].match(/(\d+\.\d+)/);
    return match ? match[0] : null;
}

// ==========================================
// 6. API 라우트
// ==========================================

// 주소 검색
app.post('/api/search', async (req, res) => {
    try {
        const { address } = req.body;
        console.log(`🔍 [검색 요청] ${address}`);
        const response = await axios.post('https://api.hyphen.im/in0004000168', {
            kindcls: '', admin_regn1: '', cls_flag: '현행', simple_address: address, detailYn: '', limitPage: '10', pageNo: '1'
        }, { headers: { 'Content-Type': 'application/json', 'Hkey': CONFIG.HKEY, 'User-Id': CONFIG.USER_ID }, timeout: 20000 });
        res.json({ success: true, list: response.data.data ? response.data.data.list : [] });
    } catch (e) { res.status(500).json({ success: false }); }
});

// 민원캐시 발급 (안정적인 예전 코드 방식)
app.post('/api/issue', async (req, res) => {
    try {
        const { uniqNo } = req.body;
        console.log(`📄 [발급 시도] 고유번호: ${uniqNo}`);
        
        const response = await axios.post('https://api.hyphen.im/in0004000948', {
            userId: CONFIG.HYPHEN_ID, userPw: CONFIG.HYPHEN_PW, userPwEnc: CONFIG.HYPHEN_PW,
            searchDiv: 'uniqNo', uniqNo: uniqNo, payDiv: '0', payNo: CONFIG.PAY_NO, payPw: CONFIG.PAY_PW, payPwEnc: CONFIG.PAY_PW,
            pdfHex: 'Y', xmlYn: 'N', display: '1', cmortCheck: '', tradeCheck: '', dupChk: '', excRegYn: '', closingYn: '', kindcls: '', kindclsYn: ''
        }, { 
            headers: { 'Content-Type': 'application/json', 'Hkey': CONFIG.HKEY, 'User-Id': CONFIG.USER_ID },
            timeout: 60000 
        });

        const result = response.data;

        if (result.common && result.common.errYn === 'Y') {
            console.log("❌ 하이픈 발급 실패:", result.common.errMsg);
            res.json({ success: false, msg: result.common.errMsg });
        } else if (result.data && result.data.pdfHexString) {
            console.log("✅ 하이픈 발급 성공");
            const outList = result.data.outList || [];
            const parsedArea = extractExclusiveArea(outList);
            res.json({ 
                success: true, 
                pdfHex: result.data.pdfHexString, 
                info: outList, 
                parsedArea 
            });
        } else {
            res.json({ success: false, msg: "데이터 수신 오류" });
        }
    } catch (e) { 
        console.error("🔥 통신 에러:", e.message); 
        res.status(500).json({ success: false, msg: '하이픈 통신 실패' }); 
    }
});

// [최종 수정] 데이터 흐름 매칭 및 템플릿 출력 강제화 로직
app.post('/api/analyze', async (req, res) => {
    try {
        let { registryData, userInputs } = req.body;
        
        // 1. 전용면적 자동 보정 (등기부 데이터 우선)
        const correctArea = extractExclusiveArea(Array.isArray(registryData) ? registryData : []);
        if (correctArea) userInputs.area = correctArea;

        // 2. AI에게 전달할 최종 명령 (Prompt) - 데이터 흐름을 명시적으로 연결
        const prompt = `
            사용자가 입력한 [리포트 데이터]를 바탕으로, 시스템 지침(System Instruction)의 [최종 리포트 템플릿] 양식을 100% 똑같이 복사하여 출력하십시오.
            
            [리포트 데이터]
            - 소유자명: ${userInputs.ownerName || '박순호'}
            - 주소: ${userInputs.address || '정보 없음'}
            - KB시세: ${userInputs.kbPrice}
            - 전용면적: ${userInputs.area}
            - 기대출 상세내역: ${JSON.stringify(userInputs.loans)}
            - 등기부 분석 데이터: ${JSON.stringify(registryData).substring(0, 8000)}

            [출력 규칙]
            1. 반드시 ──────────────────────── 구분선으로 시작하십시오.
            2. 템플릿에 정의되지 않은 서술형 문장(예: "본 리포트는...", "1. 기대출 현황" 등)은 절대 쓰지 마십시오.
            3. 시스템 지침에 정의된 [Case 1~6]의 한도 계산 로직을 실시간으로 계산하여 변수 자리에 숫자를 채우십시오.
            4. 모든 금액은 '만원' 단위를 붙이고 천 단위 콤마를 적용하십시오.
        `;

        const result = await model.generateContent(prompt);
        res.json({ success: true, analysis: result.response.text() });
    } catch (e) { 
        console.error("AI 분석 중 오류 발생:", e);
        res.status(500).json({ success: false }); 
    }
});

// 공유 URL 저장
app.post('/api/share', (req, res) => {
    try {
        const { pdfHex, aiText, metaData } = req.body;
        const reportId = crypto.randomBytes(4).toString('hex');
        const savePath = path.join(REPORT_DIR, reportId);
        if (!fs.existsSync(savePath)) fs.mkdirSync(savePath);
        fs.writeFileSync(path.join(savePath, 'data.json'), JSON.stringify({ aiText, metaData, pdfHex }));
        if (pdfHex) fs.writeFileSync(path.join(savePath, 'doc.pdf'), Buffer.from(pdfHex, 'hex'));
        res.json({ success: true, reportId });
    } catch (e) { res.status(500).json({ success: false }); }
});

const PORT = process.env.PORT || 8001; // 시스템이 주는 포트 우선 사용

app.listen(PORT, "0.0.0.0", () => {
    console.log(`🚀 서버 가동 중 (Port: ${PORT})`);
});